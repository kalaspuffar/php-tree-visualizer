fn main() {
    // Intentionally empty. This binary is a structural placeholder; behavior
    // is added by the OpenSpec changes that follow `workspace-scaffold`.
    // See SPECIFICATION.md §3.1 for the modules to come.
}
